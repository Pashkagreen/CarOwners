import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginVertical: 10,
    width: '100%',
  },
  text: {
    fontSize: 15,
    fontWeight: 'bold',
    lineHeight: 26,
  },
});

export default styles;
