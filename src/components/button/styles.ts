import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderRadius: 8,
    marginBottom: 16,
    width: '100%',
  },
  text: {
    fontSize: 14,
    fontWeight: 'bold',
  },
});

export default styles;
