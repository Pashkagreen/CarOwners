import { StyleSheet } from 'react-native';

const skeletonParameters = {
  absoluteFill: StyleSheet.absoluteFill,
  gradientColors: ['transparent', 'black', 'transparent'],
  start: { x: 0, y: 0 },
  end: { x: 1, y: 0 },
};

export default skeletonParameters;
